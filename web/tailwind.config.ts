/**
 * Dream Wave — Tailwind CSS v4 configuration note.
 *
 * Tailwind v4 is CSS-first. Theme tokens, breakpoints, and design tokens
 * live in `src/app/globals.css` (`@import "tailwindcss"`, `@theme`, `@layer`).
 * PostCSS is configured via `postcss.config.mjs`.
 *
 * A legacy `tailwind.config.js` is intentionally NOT used.
 * Extend tokens in CSS, not in a JS config file.
 */

export {};
