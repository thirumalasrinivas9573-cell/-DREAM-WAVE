import { RouteLoading } from "@/components/common/route-loading";

/**
 * Route-level loading foundation (App Router `loading.tsx`).
 */
export default function Loading() {
  return <RouteLoading label="Loading Dream Wave" />;
}
