{
  "name": "dream-wave-ai",
  "version": "1.0.0",
  "description": "Dream Wave AI — production AI career mentor, goals, learning & community platform",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently -n server,client -c teal,cyan \"npm run server\" \"npm run client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "lint": "npm run lint --prefix client",
    "arch:verify": "bash scripts/verify-boundaries.sh",
    "test": "npm test --prefix server",
    "ci": "npm run arch:verify && npm test && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "ai",
    "career",
    "mentor",
    "react",
    "express",
    "mongodb"
  ],
  "license": "MIT"
}
